//未完成
function kmp(txt = "", pat = "") {
    let dp = [];
    for (const iterator of txt) {
        dp.push(iterator)
        while (dp.length > 0) {
            let compare = dp.pop()

        }
    }
}
